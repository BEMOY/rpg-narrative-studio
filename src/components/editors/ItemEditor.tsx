// Superseded by EntryEditor.tsx — the Studio now uses one generic editor across all
// 8 categories (see docs/12_Editors.md), matching the unified Entry model in
// src/types/database.ts. Kept as an empty module so no stale imports remain.
export {};
